@model VozdelEste.Models.Noticias

@{
    ViewBag.Title = "Detalle de Noticia";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var u = Session["Usuario"] as VozdelEste.Models.Usuarios;
}

@section Styles {
    <link href="~/Styles/Views/NoticiasViews.css" rel="stylesheet" />
}

<div class="perfil-panel">
    <h2>Detalle de la Noticia</h2>

    <div class="perfil-info">
        <p><strong>Título:</strong> @Model.Titulo</p>
        <p><strong>Contenido:</strong> @Model.Contenido</p>
        <p><strong>Fecha:</strong> @Model.FechaPublicacion.ToString("dd/MM/yyyy")</p>
        @if (!string.IsNullOrEmpty(Model.Imagen))
        {
            <p><strong>Imagen:</strong></p>
            <img src="@Model.Imagen" alt="Foto conductor" style="max-width:100%; border-radius:10px; box-shadow:0 2px 6px rgba(0,0,0,0.4);" />
        }
    </div>

    <div class="perfil-actions">
        @if (u != null && u.TienePermiso("Editar noticias"))
        {
            @Html.ActionLink("Editar", "Edit", new { id = Model.Id }, new { @class = "btn btn-editar" })
        }        
        @Html.ActionLink("Volver", "Index", null, new { @class = "btn btn-eliminar" })
    </div>
</div>
